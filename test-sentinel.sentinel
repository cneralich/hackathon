import "tfplan"

pods = tfplan.resources.kubernetes_pod

mandatory_tags = [
  "derp",
]

pod_tag = rule {
  all pods as name, pods {
    all pods as index, r {
      all mandatory_tags as t {
        r.applied.metadata contains t
      }  
    }
  }
}

main = rule {
  (pod_tag) else true
}
